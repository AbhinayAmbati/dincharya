<?xml version="1.0" encoding="utf-8"?>
<!--
  Dincharya — Android manifest.
  v1 is fully local: no INTERNET permission is declared on purpose, so the app
  physically cannot send data anywhere. When the Express backend arrives in a
  later phase, add the permission and document WHY in this comment.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Needed on Android 13+ to show reminder notifications. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".DincharyaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dincharya">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Handles the Complete / Snooze buttons on reminder notifications. -->
        <receiver
            android:name=".notifications.ReminderActionReceiver"
            android:exported="false" />
    </application>
</manifest>

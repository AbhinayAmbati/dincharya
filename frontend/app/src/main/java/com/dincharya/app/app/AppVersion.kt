package com.dincharya.app.app

/**
 * The one true place the app's version name lives.
 *
 * Keep in sync with `versionName` in app/build.gradle.kts (and versionCode
 * +1). Both the Settings screen and the update checker read it from here —
 * two separate copies once drifted out of sync and made the update screen
 * report a false "new version available".
 */
const val APP_VERSION = "0.2.4"
